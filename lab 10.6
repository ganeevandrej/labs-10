#include <stdio.h>


int main()
{
	int  a, b, c;
	scanf_s("%d %d %d", &a, &b, &c);
	if ((a*a == b*b + c*c) || (c*c == a*a + b*b) || (b*b == c*c + a*a))
		printf("true");
	else
		printf("false");
	return 0;
}
