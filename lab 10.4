#include <stdio.h>


int main()
{
	int A, a, b, c;
	scanf_s("%d", &A);
	a = A % 10;
	b = A / 10 % 10;
	c = A / 100;
	if ((a > b) && (b > c) || ((a < b) && (b < c)))
		printf("true");
	else
		printf("false");
	return 0;
}
